<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="DialogAnimation">
        <item name="android:windowEnterAnimation">@android:anim/slide_in_left</item>
        <item name="android:windowExitAnimation">@android:anim/slide_out_right</item>
    </style>

    <style name="DialogAnim">
        <item name="android:windowEnterAnimation">@anim/setanim_enter</item>

        <item name="android:windowExitAnimation">@anim/setanim_exit</item>
    </style>
    <style name="Dialog_Call_Help">
        <item name="android:windowEnterAnimation">@anim/setanim_dialog_call_enter</item>
        <item name="android:windowExitAnimation">@anim/setanim_dialog_call_exit</item>
    </style>

</resources>

